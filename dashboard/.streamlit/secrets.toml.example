# Streamlit Cloud reads secrets from its settings UI, but for local testing
# you can use secrets.toml. This is an example of what to provide.
POSTGRES_USER = "your_username"
POSTGRES_PASSWORD = "your_password"
POSTGRES_HOST = "your_neon_host.neon.tech"
POSTGRES_DB = "your_database"
